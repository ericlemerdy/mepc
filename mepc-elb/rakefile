cookbook_dir = 'cookbooks/'

task :default => :clean do
  puts 'Downloading required cookbooks...'
  mkdir cookbook_dir
  cmd = "git clone https://github.com/opscode-cookbooks/varnish.git #{cookbook_dir}"
  system(cmd)
end

task :clean do
  puts 'Cleaning cookbooks...'
  rm_rf cookbook_dir
end
